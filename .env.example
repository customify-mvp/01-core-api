# Environment
ENVIRONMENT=development
DEBUG=true

# Database
DATABASE_URL=postgresql+asyncpg://customify:customify123@localhost:5432/customify_dev

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=your-super-secret-key-min-32-chars-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days
JWT_REFRESH_TOKEN_EXPIRE_MINUTES=43200  # 30 days

# AWS (optional for now)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=customify-dev

# OpenAI (optional for now)
OPENAI_API_KEY=

# Stripe (optional for now)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Shopify (optional for now)
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_WEBHOOK_SECRET=

# CORS (comma-separated or JSON array)
# Examples:
#   CORS_ORIGINS=http://localhost:3000,http://localhost:5173
#   CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=